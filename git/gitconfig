[github]
	user = kpearson

[user]
	name = Kit Pearson
	email = 3293289+kpearson@users.noreply.github.com

[core]
	editor = nvim
	excludesfile = /Users/kit/dotfiles/git/gitignore
	hooksPath = /Users/kit/dotfiles/git/git-hooks

[color]
	ui = true
	branch = auto
	diff = auto
	interactive = auto
	status = auto

[push]
	default = simple

[merge]
  tool = vscode

[mergetool "vscode"]
  cmd = code --wait $MERGED

[alias]
	lol = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
	conflicted = !nvim +Conflicted
	contributors = shortlog -s -n -e
	st = status
	co = checkout
	br = branch
	ci = commit -m
	ca = commit -am
	ps = push
	pl = pull
	df = diff
	lg = log --oneline --graph --decorate
	unstage = reset HEAD --
	last = log -1 HEAD
	show-patch = "!f() { git show $1:$2 | git apply; }; f"

[init]
	templatedir = /Users/kit/dotfiles/git/.git-templates

[diff]
    tool = default-difftool

[difftool "default-difftool"]
    cmd = code --wait --diff $LOCAL $REMOTE

[mergetool]
	keepBackup = false
