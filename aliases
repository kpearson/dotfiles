# alias Gwho="git log --all --format='%cN %Cblue<%cE>%Creset' | sort -u"
alias todo="vim ~/.todo.md"
alias job="vim $HOME/.jobs.md"

# global
alias vi="nvim"
alias reload=". ~/.zshrc && echo 'ZSH reloaded!'"
alias zshr="vim ~/.zshrc && reload"
alias vimr="vim ~/.vimrc && . $HOME/.vimrc"
alias zshs="subl ~/.zshrc && reload"
alias te="cd $HOME/code/my_projects/trial-and-error/ && vim"
alias t="tmux"
alias tl="tmux ls"

# Cyndx | Docker 4/28/17
alias mrs="make rebuild_and_seed"
alias docker_rollback="dcr -e RAILS_ENV=test app rails db:rollback && dcr app rails db:rollback"

alias aliases="vim $HOME/dotfiles/aliases"

alias tailc="tailcassandra"
